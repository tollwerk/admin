<VirtualHost *:{{port}}>
    Protocols h2 http/1.1{{#ssl}}
    Include {{configroot}}/ssl_{{primary_domain}}.include{{/ssl}}
    Include {{configroot}}/vhost_{{primary_domain}}.include
</VirtualHost>
