doctrine:
  dbparams:
    driver: "pdo_mysql"
    user: "root"
    password: ""
    dbname: "admin_test"
    host: "localhost"
apache:
  group: "apache"
  service: "service"
  reload: ["apache2", "graceful"]
  restart: ["apache2", "restart"]
php:
  service: "/etc/init.d/php-fpm-php%s"
  reload: ["restart"]
  restart: ["restart"]
general:
  locale: de_DE
  group: account
  basedir: "/home/travis/build/tollwerk/admin/tmp"
  fpmdir: "/var/run/php-fpm"
  certdir: "/etc/letsencrypt/live"
  certemail: "cert@example.com"
