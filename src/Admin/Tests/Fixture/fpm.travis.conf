;; warmup

[warmup]
listen = 127.0.0.0:9001
listen.owner = nobody
listen.group = nobody
listen.mode = 0660
user = nobody
group = nobody
pm = dynamic
pm.start_servers = 3
pm.max_children = 100
pm.min_spare_servers = 2
pm.max_spare_servers = 5
pm.max_requests = 10000
request_terminate_timeout = 300
